@model IEnumerable<string>

<div class="menuContainer">
    @if (User.Identity.IsAuthenticated && (User.IsInRole("Admin") || User.IsInRole("Librarian")))
    {
        <nav class="leftMenu">
            <span class="navCategoryText">Admin Panel</span>
            <ul>
                @if (User.Identity.IsAuthenticated && User.IsInRole("Librarian"))
                {
                    <li>@Html.ActionLink("Book management", "Index", "Librarian")</li>
                }

                @if (User.Identity.IsAuthenticated && User.IsInRole("Admin"))
                {
                    <li>Roles Management
                        <ul class="leftMenuSecond">
                            <li>@Html.ActionLink("Add role", "CreateRole", "Roles")</li>
                            <li>@Html.ActionLink("List of roles", "List", "Roles")</li>
                        </ul>
                    </li>
                    <li>User Management
                        <ul>
                            <li>@Html.ActionLink("Manage users", "Index", "Admin")</li>
                        </ul>
                    </li>
                }

            </ul>
        </nav>
    }

    @if (User.Identity.IsAuthenticated)
    {
        <nav class="leftMenu">
            <span class="navCategoryText">Library</span>
            <ul>
                <li>@Html.ActionLink("Bookshelf", "Index", "Shelf")</li>
            </ul>
        </nav>
    }

    <nav class="leftMenu">
        <span class="navCategoryText">Category</span>
        <ul>
            <li>
                @Html.RouteLink("Default",new { controller = "Book", action = "List"}, new { @class = "menuItem"})
            </li>
            @foreach (string x in Model)
            {
                <li>
                    @Html.RouteLink(
                    x,
                    new
                    {
                        controller = "Book",
                        action = "List",
                        genre = x,
                        page = 1
                    },
                    new
                    {
                        @class = "menuItem" +
                        (ViewBag.genre == x ? " menuItemSelected" : "")
                    }
                    )
                </li>
            }
        </ul>
    </nav>
</div>